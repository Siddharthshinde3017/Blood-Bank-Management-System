admin login function :  def admin() --> def admin_login()
if admin and admin["password"] == password: --> if admin and check_password_hash(admin["password"], password):
boilerplate code for all templates removed because we use base html file in all
decorator admin,user,,doner,required** added in app.py
manage doners added